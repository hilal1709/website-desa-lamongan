"use client"
type Props = { years: number[]; hamlets: string[]; year: number | "all"; dusun: string; onYear: (value: number | "all") => void; onDusun: (value: string) => void }

export function FilterBar({ years, hamlets, year, dusun, onYear, onDusun }: Props) { const input = "mt-2 min-h-12 w-full rounded-xl border border-slate-200 bg-white px-4 text-slate-900 outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-100"; return <section className="rounded-3xl bg-white p-4 shadow-sm ring-1 ring-slate-100 sm:p-5"><div className="grid gap-3 sm:grid-cols-2 sm:gap-4"><label className="text-sm font-bold text-slate-700">Dusun<select value={dusun} onChange={(event) => onDusun(event.target.value)} className={input}><option value="all">Semua dusun</option>{hamlets.map((item) => <option key={item} value={item}>{item}</option>)}</select></label><label className="text-sm font-bold text-slate-700">Tahun<select value={year} onChange={(event) => onYear(event.target.value === "all" ? "all" : Number(event.target.value))} className={input}><option value="all">Semua tahun</option>{years.map((item) => <option key={item} value={item}>{item}</option>)}</select></label></div></section> }
