import { BriefcaseBusiness, GraduationCap, Sprout, Store } from "lucide-react"
const formatter = new Intl.NumberFormat("id-ID")
export function EconomicCards({ values }: { values: { umkm: number; farmers: number; formal: number; educators: number } }) { const cards = [["UMKM Aktif", values.umkm, Store], ["Petani", values.farmers, Sprout], ["Pekerja Formal", values.formal, BriefcaseBusiness], ["Tenaga Pendidik", values.educators, GraduationCap]] as const; return <section className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([label, value, Icon]) => <div key={label} className="rounded-3xl bg-gradient-to-br from-white to-emerald-50 p-5 shadow-sm ring-1 ring-emerald-100"><Icon className="text-emerald-700"/><p className="mt-5 text-3xl font-bold text-slate-950">{formatter.format(value)}</p><p className="mt-1 text-sm font-semibold text-slate-600">{label}</p></div>)}</section> }
