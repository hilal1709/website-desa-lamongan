"use client"
import { Bar, BarChart, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts"
const colors = ["#2563eb", "#059669", "#f59e0b", "#7c3aed", "#ec4899", "#0ea5e9", "#84cc16", "#64748b"]
export function OccupationChart({ data }: { data: { name: string; total: number }[] }) { const top = [...data].sort((a, b) => b.total - a.total).slice(0, 5); return <div className="grid gap-5 lg:grid-cols-2"><div className="h-80"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="total" nameKey="name" innerRadius="55%" outerRadius="80%">{data.map((item, index) => <Cell key={item.name} fill={colors[index % colors.length]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div><div className="h-80"><ResponsiveContainer width="100%" height="100%"><BarChart data={top} layout="vertical" margin={{ left: 30 }}><XAxis type="number" hide/><YAxis dataKey="name" type="category" width={105} tick={{ fontSize: 11 }} tickLine={false} axisLine={false}/><Tooltip/><Bar dataKey="total" fill="#2563eb" radius={[0, 8, 8, 0]}/></BarChart></ResponsiveContainer></div></div> }
