import { StatusBadge } from "./status-badge"
interface Row { id?: string; title: string; meta: string; status?: string }
export function DataTable({ rows, emptyMessage = "Belum ada data." }: { rows: Row[]; emptyMessage?: string }) { return <div className="overflow-hidden rounded-3xl border border-slate-200 bg-white"><div className="grid grid-cols-[minmax(0,1fr)_auto] gap-3 bg-slate-50 px-3 py-3 text-xs font-bold uppercase tracking-wider text-slate-500 sm:px-5 sm:py-4"><span>Informasi</span><span>Status</span></div>{rows.length ? rows.map(row => <div key={row.id ?? row.title} className="grid grid-cols-[minmax(0,1fr)_auto] items-center gap-3 border-t px-3 py-4 sm:gap-4 sm:px-5"><div className="min-w-0"><p className="break-words font-bold text-slate-800">{row.title}</p><p className="mt-1 break-words text-sm text-slate-500">{row.meta}</p></div>{row.status && <StatusBadge>{row.status}</StatusBadge>}</div>) : <p className="border-t px-3 py-8 text-center text-sm font-medium text-slate-500 sm:px-5">{emptyMessage}</p>}</div> }
