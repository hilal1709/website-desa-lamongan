import Link from "next/link"; import type { NewsItem } from "@/types"
export function NewsCard({ item, featured = false }: { item: NewsItem; featured?: boolean }) { return <Link href="/berita" className={`group overflow-hidden rounded-3xl bg-white shadow-sm ring-1 ring-slate-100 ${featured ? "md:col-span-2" : ""}`}><div className={`overflow-hidden ${featured ? "aspect-[16/8]" : "aspect-[4/3]"}`}><img src={item.image} alt="" className="size-full object-cover transition duration-500 group-hover:scale-105"/></div><div className="p-5"><div className="text-xs font-bold uppercase tracking-wider text-emerald-600">{item.category} <span className="ml-2 font-medium text-slate-400">{item.date}</span></div><h3 className={`mt-3 font-bold leading-snug text-slate-900 group-hover:text-blue-600 ${featured ? "text-xl md:text-2xl" : ""}`}>{item.title}</h3>{featured && <p className="mt-3 text-sm leading-6 text-slate-500">{item.excerpt}</p>}</div></Link> }
