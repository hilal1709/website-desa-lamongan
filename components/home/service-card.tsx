import Link from "next/link"; import type { Service } from "@/types"
const tones = { blue: "bg-green-50 text-green-700", emerald: "bg-emerald-50 text-emerald-600", amber: "bg-amber-50 text-amber-600" }
export function ServiceCard({ service }: { service: Service }) { const Icon = service.icon; return <Link href={service.href} className="group rounded-3xl border border-slate-100 bg-white p-6 shadow-sm transition duration-300 hover:-translate-y-1 hover:border-green-100 hover:shadow-xl hover:shadow-green-900/5"><span className={`grid size-12 place-items-center rounded-2xl ${tones[service.tone ?? "blue"]}`}><Icon size={23}/></span><h3 className="mt-5 font-bold text-slate-900 group-hover:text-green-700">{service.title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{service.description}</p><span className="mt-5 inline-block text-sm font-bold text-green-700">Akses layanan →</span></Link> }
