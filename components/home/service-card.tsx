import Link from "next/link"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import type { Service } from "@/types"
const tones = { blue: "bg-green-50 text-green-700", emerald: "bg-emerald-50 text-emerald-600", amber: "bg-amber-50 text-amber-600" }
export function ServiceCard({ service }: { service: Service }) { const Icon = service.icon; return <Link href={service.href} className="home-service-card group"><Card className="h-full rounded-3xl border-slate-100 shadow-sm transition duration-300 group-hover:-translate-y-1 group-hover:border-green-100 group-hover:shadow-xl group-hover:shadow-green-900/5"><CardHeader><span className={`grid size-12 place-items-center rounded-2xl ${tones[service.tone ?? "blue"]}`}><Icon size={23}/></span><CardTitle className="mt-4 group-hover:text-green-700">{service.title}</CardTitle></CardHeader><CardContent>{service.description && <p className="text-sm leading-6 text-slate-500">{service.description}</p>}<span className="mt-5 inline-block text-sm font-bold text-green-700">Akses layanan →</span></CardContent></Card></Link> }
