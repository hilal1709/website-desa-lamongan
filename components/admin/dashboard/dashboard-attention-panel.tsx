import Link from "next/link"

import { ChevronRight, FileIcon, MessageIcon, NewsIcon } from "@/components/admin/dashboard-icons"
import { DashboardSectionHeading } from "@/components/admin/dashboard/section-heading"
import { Card } from "@/components/ui/card"
import type { AdminAttentionGroup } from "@/lib/admin-data"

const attentionStyles = { complaints: { icon: MessageIcon, tone: "bg-amber-50 text-amber-700" }, services: { icon: FileIcon, tone: "bg-emerald-50 text-emerald-700" }, drafts: { icon: NewsIcon, tone: "bg-sky-50 text-sky-700" } }

export function AdminAttentionPanel({ attention = [] }: { attention?: AdminAttentionGroup[] }) { return <section data-admin-reveal aria-labelledby="perlu-ditindaklanjuti-title" className="mt-5 min-w-0"><Card className="rounded-[28px] border-emerald-100 p-4 shadow-sm sm:p-6"><DashboardSectionHeading eyebrow="Prioritas operator" title="Perlu ditindaklanjuti" titleId="perlu-ditindaklanjuti-title" description="Aduan, pengajuan, dan konten yang belum selesai." /><ul className="mt-5 grid gap-4 xl:grid-cols-3">{attention.map((group) => { const { icon: Icon, tone } = attentionStyles[group.key]; return <li key={group.key} className="min-w-0"><article data-admin-card className="h-full min-w-0 rounded-2xl border border-slate-200 bg-slate-50/70 p-4 transition-shadow hover:shadow-md"><div className="flex items-start justify-between gap-3"><span data-admin-card-icon className={`grid h-10 w-10 shrink-0 place-items-center rounded-xl ${tone}`}><Icon className="h-5 w-5" /></span><data value={group.count} className="shrink-0 rounded-full bg-white px-3 py-1 text-sm font-black text-slate-950 shadow-sm">{group.count}</data></div><h3 className="mt-4 break-words font-black text-slate-950">{group.label}</h3><p className="mt-1 text-sm leading-5 text-slate-500">{group.description}</p><div className="mt-4 min-h-20">{group.items.length ? <ul className="space-y-2">{group.items.map((item) => <li key={item.id} className="min-w-0 rounded-xl bg-white px-3 py-2"><div className="flex min-w-0 flex-col gap-1 sm:flex-row sm:items-start sm:justify-between sm:gap-2"><p className="min-w-0 break-words text-sm font-bold leading-5 text-slate-800 sm:line-clamp-1">{item.title}</p><span className="w-fit max-w-full shrink-0 break-words rounded-full bg-slate-100 px-2 py-0.5 text-[10px] font-bold uppercase leading-4 tracking-wide text-slate-600">{item.status}</span></div><p className="mt-1 break-words text-xs leading-4 text-slate-500 sm:truncate">{item.meta}</p></li>)}</ul> : <p className="rounded-xl border border-dashed border-slate-200 bg-white px-3 py-4 text-center text-sm text-slate-500">Tidak ada yang perlu ditangani.</p>}</div><Link data-admin-action href={group.href} className="mt-4 inline-flex min-h-11 items-center gap-1 text-sm font-bold text-emerald-700 hover:text-emerald-800">Buka pengelolaan <ChevronRight className="h-4 w-4" /></Link></article></li> })}</ul></Card></section> }
